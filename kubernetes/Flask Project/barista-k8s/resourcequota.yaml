apiVersion: v1
kind: ResourceQuota
metadata:
  name: barista-quota
  namespace: barista
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    pods: "10"