AWS
    IAM User with ACCESS Keys
    ECR Registry(Docker Registry from AWS)

Plugins
    Docker
    Docker Pipeline
    ECR
    aws sdk for credentials

Jenkins
    Store aws keys(Credentials)
    Install docker engine in jenkins node
    Add jenkins user to docker group and reboot
    Install aws cli on node

ECS
 
